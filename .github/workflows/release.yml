# BIRD.nvim GitHub Actions Release Workflow
name: Release

on:
  push:
    tags:
      - "nvim-v*.*.*"

permissions:
  contents: write

jobs:
  release:
    name: Create GitHub Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Extract version from tag
        id: version
        run: |
          TAG="${{ github.ref_name }}"
          VERSION=${TAG#nvim-v}
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "tag_name=$TAG" >> "$GITHUB_OUTPUT"
          echo "Version: $VERSION"

      - name: Generate release notes
        id: release_notes
        run: |
          cat > release_notes.md << 'EOF'
          ## BIRD.nvim ${{ steps.version.outputs.version }}

          ### Changes
          See the [commit history](https://github.com/bird-chinese-community/BIRD.nvim/commits/${{ steps.version.outputs.tag_name }}) for details.

          ### Installation

          Using lazy.nvim:
          ```lua
          {
            "bird-chinese-community/BIRD.nvim",
            tag = "${{ steps.version.outputs.tag_name }}",
            lazy = false,
            config = function()
              require("bird2").setup()
            end,
          }
          ```
          EOF
          cat release_notes.md

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag_name }}
          name: BIRD.nvim ${{ steps.version.outputs.version }}
          body_path: release_notes.md
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
